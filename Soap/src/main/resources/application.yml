spring:
  application:
    name: server-monitoring-soap


  datasource:
    url: jdbc:mysql://localhost:3306/server_monitoring?createDatabaseIfNotExist=true
    username: root
    password: MyPassword!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080